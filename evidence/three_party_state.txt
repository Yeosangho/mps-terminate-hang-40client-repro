=== State of all three parties, 35-90 minutes into the hang ===

nvidia-cuda-mps-control -d   (the daemon, pid 135761)
  main thread : __skb_wait_for_more_packets   <- the DAEMON is blocked in recv
  thread 2    : pipe_read
  4 open sockets (inside the container's netns, so the peer could not be identified)

nvidia-cuda-mps-control      (the hung CLI, pid 140330)
  main thread : futex_wait_queue
  thread 2    : __skb_wait_for_more_packets   <- waiting for the daemon's reply
  1 open socket

nvidia-cuda-mps-server       (pid 138634)
  pipe_read                                   <- idle, nothing to do

Reading: the CLI is not slow -- **the daemon cannot produce a reply**.
The server is sitting idle in pipe_read, so this is not a GPU operation
that has yet to finish. The daemon is waiting to receive something that
never arrives.

Full kernel stack of the daemon's main thread:

  __skb_wait_for_more_packets
  __unix_dgram_recvmsg
  unix_seqpacket_recvmsg
  sock_recvmsg
  ____sys_recvmsg
  ___sys_recvmsg
  __sys_recvmsg
  __x64_sys_recvmsg
  x64_sys_call
  do_syscall_64
  entry_SYSCALL_64_after_hwframe
